import * as ort from 'https://cdn.jsdelivr.net/npm/onnxruntime-web@1.23.0/dist/ort.min.mjs';

const MODEL_URL='https://huggingface.co/hermitkk/alphabet-classifier/resolve/main/outputs/exports/alphanumeric_model.onnx';
const CLASSES=[...'0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ','blank'];
const LOCAL_DELAY=115,REMOTE_DELAY=1050,LOCAL_ACCEPT=.62;
ort.env.wasm.wasmPaths='https://cdn.jsdelivr.net/npm/onnxruntime-web@1.23.0/dist/';

export function createAIBoard({ink,semantic,getStrokes,setStrokes,onState}){
 let enabled=false,localTimer=null,remoteTimer=null,session=null,sessionPromise=null,elements=[],history=[],pendingStart=0,revision=0,lastRemoteRevision=0;
 const sctx=semantic.getContext('2d');
 const endpoint=()=>window.AIRBOARD_AI_ENDPOINT||localStorage.getItem('airboard-ai-endpoint')||'https://airboard-ai.doppiaelletech.workers.dev/';
 const clone=x=>structuredClone(x);
 function loadModel(){if(session)return Promise.resolve(session);if(sessionPromise)return sessionPromise;onState('local-loading');sessionPromise=ort.InferenceSession.create(MODEL_URL,{executionProviders:['wasm'],graphOptimizationLevel:'all'}).then(s=>{session=s;onState(enabled?'ready':'off');return s}).catch(e=>{console.warn('Local handwriting model unavailable',e);onState('local-fallback');return null});return sessionPromise}
 function toggle(){enabled=!enabled;clearTimeout(localTimer);clearTimeout(remoteTimer);if(enabled){pendingStart=getStrokes().length;loadModel();onState('ready')}else onState('off');return enabled}
 function isEnabled(){return enabled}
 function bounds(ss){const pts=ss.flatMap(s=>s.points);if(!pts.length)return null;const xs=pts.map(p=>p.x),ys=pts.map(p=>p.y);return{x:Math.min(...xs),y:Math.min(...ys),w:Math.max(2,Math.max(...xs)-Math.min(...xs)),h:Math.max(2,Math.max(...ys)-Math.min(...ys))}}
 function heuristic(ss,b){const n=ss.length,ar=b.w/b.h;if(n===1){if(ar>3.4)return{char:'−',confidence:.98};if(ar<.18)return{char:'1',confidence:.76}}if(n===2){const bs=ss.map(s=>bounds([s]));if(bs.every(x=>x.w/Math.max(x.h,1)>2.5)&&Math.abs(bs[0].x-bs[1].x)<Math.max(bs[0].w,bs[1].w)*.45)return{char:'=',confidence:.98};const horiz=bs.filter(x=>x.w/x.h>2.2).length,vert=bs.filter(x=>x.h/x.w>2.2).length;if(horiz&&vert)return{char:'+',confidence:.94}}return null}
 function raster(ss,b){const size=96,pad=12,c=document.createElement('canvas');c.width=c.height=size;const x=c.getContext('2d',{willReadFrequently:true});x.fillStyle='#fff';x.fillRect(0,0,size,size);const scale=Math.min((size-pad*2)/b.w,(size-pad*2)/b.h),ox=(size-b.w*scale)/2-b.x*scale,oy=(size-b.h*scale)/2-b.y*scale;x.strokeStyle='#000';x.lineWidth=Math.max(3,5*scale/Math.max(scale,1));x.lineCap='round';x.lineJoin='round';for(const s of ss){x.beginPath();s.points.forEach((p,i)=>i?x.lineTo(p.x*scale+ox,p.y*scale+oy):x.moveTo(p.x*scale+ox,p.y*scale+oy));x.stroke()}const d=x.getImageData(0,0,size,size).data,a=new Float32Array(size*size);for(let i=0;i<a.length;i++)a[i]=(d[i*4]/255-.5)/.5;return a}
 function softmaxTop(logits){let m=-Infinity;for(const v of logits)m=Math.max(m,v);let sum=0;const p=new Float32Array(logits.length);for(let i=0;i<logits.length;i++){p[i]=Math.exp(logits[i]-m);sum+=p[i]}let ids=[...p.keys()].sort((a,b)=>p[b]-p[a]).slice(0,3);return ids.map(i=>({char:CLASSES[i],confidence:p[i]/sum}))}
 async function classify(ss){const b=bounds(ss);if(!b)return null;const h=heuristic(ss,b);if(h)return{...h,bounds:b,alternatives:[h]};const s=await loadModel();if(!s)return null;try{const input=new ort.Tensor('float32',raster(ss,b),[1,1,96,96]);const out=await s.run({[s.inputNames[0]]:input});const top=softmaxTop(out[s.outputNames[0]].data);return{...top[0],bounds:b,alternatives:top}}catch(e){console.warn('Local recognition failed',e);return null}}
 function schedule(){if(!enabled)return;clearTimeout(localTimer);localTimer=setTimeout(processPending,LOCAL_DELAY)}
 async function processPending(){if(!enabled)return;const all=getStrokes(),ss=all.slice(pendingStart);if(!ss.length)return;const snapshot=clone(all),result=await classify(ss);if(!enabled)return;if(!result||result.char==='blank'||result.confidence<LOCAL_ACCEPT){scheduleRemote(ss,bounds(ss));return}history.push({strokes:snapshot,elements:clone(elements),pendingStart});elements.push({id:crypto.randomUUID?.()||String(Date.now()),type:'glyph',content:result.char,confidence:result.confidence,alternatives:result.alternatives,bounds:result.bounds,revision:++revision});setStrokes(all.slice(0,pendingStart));pendingStart=getStrokes().length;render();onState('local-ready');scheduleRemote(ss,result.bounds)}
 function scheduleRemote(raw,b){clearTimeout(remoteTimer);remoteTimer=setTimeout(()=>reconcile(raw,b),REMOTE_DELAY)}
 function tokenContext(){return elements.slice(-24).map(e=>({content:e.content,confidence:e.confidence,alternatives:e.alternatives?.map(a=>a.char).slice(0,3)}))}
 function context(){return elements.slice(-24).map(e=>({type:e.type,content:e.content,confidence:e.confidence}))}
 function batchImage(raw,b){const c=document.createElement('canvas');c.width=640;c.height=180;const x=c.getContext('2d');x.fillStyle='#08090c';x.fillRect(0,0,c.width,c.height);x.fillStyle='#fff';x.font='600 64px system-ui,sans-serif';x.textBaseline='middle';const text=elements.slice(-18).map(e=>e.content).join('');x.fillText(text,24,90,590);return c.toDataURL('image/png')}
 async function reconcile(raw,b){if(!enabled||revision===lastRemoteRevision||!elements.length)return;const sentRevision=revision;onState('thinking');try{const r=await fetch(endpoint(),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({image:batchImage(raw,b),context:[...context(),{type:'local_candidates',content:JSON.stringify(tokenContext())}],locale:document.documentElement.lang,bounds:b||{x:0,y:0,w:1,h:1}})});if(!r.ok)throw new Error(`AI ${r.status}`);const out=await r.json();lastRemoteRevision=sentRevision;if(out?.content&&Number(out.confidence||0)>=.72){const recent=elements.filter(e=>e.revision<=sentRevision).slice(-18);if(recent.length){const first=recent[0],last=recent.at(-1),bb={x:first.bounds.x,y:Math.min(...recent.map(e=>e.bounds.y)),w:(last.bounds.x+last.bounds.w)-first.bounds.x,h:Math.max(...recent.map(e=>e.bounds.y+e.bounds.h))-Math.min(...recent.map(e=>e.bounds.y))};history.push({strokes:clone(getStrokes()),elements:clone(elements),pendingStart});const ids=new Set(recent.map(e=>e.id));elements=elements.filter(e=>!ids.has(e.id));elements.push({...out,id:String(Date.now()),bounds:bb,revision:sentRevision,validated:true});render()}}onState('ready')}catch(e){console.warn('Async AI reconciliation failed',e);onState('local-ready')}}
 function render(){sctx.clearRect(0,0,semantic.clientWidth,semantic.clientHeight);sctx.save();sctx.fillStyle='#eefcff';sctx.strokeStyle='#eefcff';sctx.lineWidth=2;for(const e of elements){const b=e.bounds;if(!b)continue;if(e.type==='geometry'&&e.shape==='triangle'){sctx.beginPath();sctx.moveTo(b.x+b.w/2,b.y);sctx.lineTo(b.x,b.y+b.h);sctx.lineTo(b.x+b.w,b.y+b.h);sctx.closePath();sctx.stroke();continue}const glyph=e.type==='glyph';const fs=glyph?Math.max(24,Math.min(58,b.h*1.12)):Math.max(22,Math.min(44,b.h*.72));sctx.font=`600 ${fs}px Inter,system-ui,sans-serif`;sctx.textBaseline='middle';sctx.fillText(e.content,b.x,b.y+b.h/2,Math.max(80,glyph?b.w*2.2:b.w*1.35))}sctx.restore()}
 function undoSemantic(){if(!history.length)return false;const h=history.pop();elements=h.elements;pendingStart=h.pendingStart;setStrokes(h.strokes);revision++;render();return true}
 function clear(){clearTimeout(localTimer);clearTimeout(remoteTimer);elements=[];history=[];pendingStart=0;revision=0;lastRemoteRevision=0;render()}
 function resize(){render()}
 return{toggle,isEnabled,schedule,undoSemantic,clear,resize,render}
}
